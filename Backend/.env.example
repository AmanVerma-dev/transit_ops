# Database Settings (Local / Docker Compose config)
DATABASE_URL=sqlite:///./transitops.db
# For Docker Compose Postgres, use:
# DATABASE_URL=postgresql://postgres:postgrespassword@db:5432/transitops

# Security Settings
JWT_SECRET_KEY=supersecretjwtkeythatshouldbechangedinproduction123!
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
PROJECT_NAME="TransitOps Backend API"
